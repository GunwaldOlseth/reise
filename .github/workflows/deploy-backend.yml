name: Deploy backend

on:
  push:
    branches: [master]
    paths:
      - 'backend/**'
      - '.github/workflows/deploy-backend.yml'
  workflow_dispatch:

concurrency:
  group: deploy-backend
  cancel-in-progress: true

env:
  GCP_PROJECT: homey-376215
  GCP_REGION: europe-north1
  SERVICE: reise-backend

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: backend/api/go.mod
          cache-dependency-path: backend/api/go.sum

      - name: Check deploy secret
        run: |
          if [ -z "${{ secrets.SA_REISE }}" ]; then
            echo "::error::SA_REISE is not set. Add the deploy service account JSON in GitHub → Settings → Secrets → Actions, or run: bash scripts/setup-github-actions.sh <key.json>"
            exit 1
          fi

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v3
        with:
          credentials_json: ${{ secrets.SA_REISE }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v3

      - name: Verify backend build
        working-directory: backend/api
        run: |
          go build -o /tmp/reise-backend .

      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy "$SERVICE" \
            --source backend/api \
            --region "$GCP_REGION" \
            --project "$GCP_PROJECT" \
            --allow-unauthenticated \
            --update-env-vars="FIREBASE_PROJECT_ID=${GCP_PROJECT}" \
            --quiet

      - name: Show service URL
        run: |
          gcloud run services describe "$SERVICE" \
            --region "$GCP_REGION" \
            --project "$GCP_PROJECT" \
            --format='value(status.url)'
